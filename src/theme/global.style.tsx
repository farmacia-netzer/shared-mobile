import { StyleSheet } from 'react-native';
import { LARGE_PADDING } from './dimensions.constant';

export const globalStyle = StyleSheet.create({
  GLOBAL_MARGIN: {
    marginHorizontal: LARGE_PADDING
  },
  cardStyle: {}
});
